{{> nav-partial }}

{{> header-partial }}


<main>
    <div class="container">
        <div class="row">

            {{#each products}}
            {{> product-partial this}}
            {{/each}}


        </div>
    </div>
    <br><br>
</main>

{{> footer-partial }}